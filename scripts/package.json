{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed": "tsx ./src/seed.ts",
    "reset-platform": "tsx ./src/reset-platform.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "@clerk/backend": "^3.5.0",
    "@workspace/db": "workspace:*"
  }
}